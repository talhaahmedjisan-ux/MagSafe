<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MsgSafe"
        android:supportsRtl="true"
        android:theme="@style/Theme.MsgSafe">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="MsgSafe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Notification Listener: catches every notification the phone receives -->
        <service
            android:name=".service.MsgSafeNotificationListener"
            android:exported="true"
            android:foregroundServiceType="specialUse"
            android:label="MsgSafe Catcher"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Catches Messenger notifications to preserve deleted messages locally" />
        </service>

        <!-- Keeps the listener alive as a foreground service (helps survive Game Space / battery kill) -->
        <service
            android:name=".service.KeepAliveForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

    </application>
</manifest>
